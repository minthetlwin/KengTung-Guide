import { mmmHeroImage, mmmGalleryAlbums } from './maharMyatMuni'
import { naungTungImage } from './images'

// Index-aligned with `news.articles` in each locale file.
export const newsImages: string[] = [mmmHeroImage, mmmGalleryAlbums[0].images[0], naungTungImage]
