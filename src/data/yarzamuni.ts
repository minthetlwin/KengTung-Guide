import { heroImages } from './images'

export const yzmHeroImage = heroImages.yarzamuni

// Supplied directly by the project owner; audio extracted from the source
// .mov (a black video track wrapping a voice recording) and re-encoded to
// MP3 for universal browser playback.
export const yzmHistoryAudioSrc = '/audio/yzm-history-narration.mp3'

// No published GPS fix exists for this statue; estimated from its
// "Pan Kwai village, east of Keng Tung" description — flagged as
// approximate in the UI.
export const yzmCoordinates = { lat: 21.111, lng: 99.663 }
